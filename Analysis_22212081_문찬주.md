
![logo](images/logo.png)

## Revision History

| Revision date | Version # | Description | Author |
| --- | --- | --- | --- |
| 2026-04-19 | 0.0.1 | 초기 분석 보고서 작성 완료 | 문찬주 |
| 2026-05-02 | 0.0.2 | UI 프로토타입 상세화 및 보고서 정리 | 문찬주 |
| 2026-06-11 | 0.0.3 | 지나침 알림·안내 중 화면 유지·스크린리더 접근성 반영 | 문찬주 |
| 2026-06-12 | 0.0.4 | 경로 총 거리 표시·도착 시각 예상값 명시·도메인 모델 정합(userId 제거) | 문찬주 |

---

## Contents

1. Introduction
2. Use Case Analysis
3. Domain Analysis
4. Functional Decisions
5. User Interface Prototype
6. Glossary
7. References

---

## 1. Introduction

### 1) Summary

우리는 목적지에 가기 위해 대중교통을 이용한다. 하지만 신체적 노화로 인해 주의력이 감소한 노인들은 안내 방송을 놓치거나, 복잡한 노선도에 당황하여 하차 시점을 놓치는 곤란을 겪곤 한다. 기존 앱들은 젊은 층에 최적화되어 있어 노인들이 직관적으로 하차 시점을 인지하기 어렵다. 또한 지하 공간에서의 GPS 오차는 사용자에게 부정확한 정보를 제공할 위험이 크다.

이에 따라 "어디가수?"는 위치 추적의 신뢰도가 높은 **지상 대중교통**으로 범위를 한정하고, 노인들의 인지 특성을 반영한 직관적인 UI와 강력한 진동 알림을 제공하는 시스템을 고안했다. 이를 통해 노인들이 하차를 놓쳐 겪는 심리적 불안을 줄이고, 안전한 이동권을 보장하고자 한다.

### 2) Describe the features of project

- **지상 특화 경로 탐색:** 전국 광역시(서울·인천·부산·대구·광주·대전·울산·세종) 내 지상 대중교통 중심의 최적 경로 계산.
- **실시간 지오펜싱(Geofencing) 알림:** 목적지 및 환승지 근접 시 화면 점유 알림 및 진동 피드백 제공. 도착 반경을 건너뛰고 정류장을 지나친 경우에도 "지나쳤어요 · 다음 정류장에서 내리세요" 알림으로 보강한다.
- **통합 경로 진행 바:** 전체 경로와 현재 위치, 환승 지점을 하나의 선형 UI로 시각화.
- **환승 자동 경로 업데이트:** 환승 지점 하차 안내 후 카운트다운(기본 8초)을 거쳐 별도 조작 없이 다음 구간 안내로 자동 전환(사용자가 직접 확인도 가능).
- **안내 중 화면 유지·접근성:** 안내 중에는 화면 잠금(Screen Wake Lock)을 유지하여 화면 꺼짐으로 알림이 멈추지 않게 하고, 모든 알림 문구를 음성과 함께 `aria-live` 영역으로 출력하여 저시력 사용자의 스크린리더(보이스오버/톡백)에 전달한다.

---

## 2. Use Case Analysis

### 1) Use Case Diagram

![Use Case Diagram](images/usecase_diagram.png)

** 어디가수? Use Case Diagram **

- **Actors:** User(어르신), System, GPS Sensor, ODsay API.
- **핵심 Use Case:** Select Region, Search & Select Route, Calculate Route, Start Navigation, Track Real-time Location, Display Linear Progress Bar, Provide Arrival Alert(Pre-Alert / Final Alert), Automatic Route Update
- **관계 표기:** 항상 수행되는 하위 단계는 `<<include>>`, 조건부 발생(목적지 근접 / 환승 발생)은 `<<extend>>`



### 2) Use Case Description (핵심 사례 요약)

#### Use Case #1 : Search & Select Route

- **Summary:** 사용자가 지역을 선택하고 지상 대중교통 중심의 경로를 확인하는 기능
- **Primary Actor:** User
- **Preconditions:** 통신 가능 상태, 지역 선택(서울·부산·대구 등 광역시) 완료
- **Main Success Scenario:**
  1. 사용자가 출발지와 도착지를 입력한다.
  2. 시스템은 외부 API를 호출하여 지상 대중교통 중심의 경로를 탐색한다.
  3. 시스템은 환승이 적은 경로를 큰 글씨로 정렬하여 표시한다.
  4. 사용자가 특정 경로를 선택하면 종료된다.

#### Use Case #2 : Provide Arrival Alert

- **Summary:** 하차 정류장 접근 시 진동과 시각적 알림을 제공하는 기능
- **Primary Actor:** System
- **Preconditions:** 실시간 위치 추적 모드 활성화
- **Main Success Scenario:**
  1. 시스템은 실시간 GPS 좌표와 목적지 좌표 사이의 거리를 계산한다.
  2. **[하차역 약 450m 전 / 데모 모드는 1정거장 전]** 하단 바텀 시트가 슬라이드 업되며 예고 진동을 발생시킨다.
  3. **[목적지 도착]** 화면 색상 반전 및 강력한 연속 진동을 발생시킨다.
  4. 사용자가 확인 버튼을 누르면 종료된다.
- **Extension:**
  - **[하차역 지나침]** 도착 반경(70m)을 건너뛰어 하차역을 지나친 경우(최근접 거리가 가까워졌다가 다시 멀어짐), "지나쳤어요 · 다음 정류장에서 내리세요"로 안내한다(환승역을 지나친 경우 환승 재안내).
  - 모든 알림 문구는 음성과 함께 `aria-live` 영역으로 출력되어 스크린리더 사용자에게 전달되며, 안내 중에는 화면 잠금(Wake Lock)을 유지한다.

#### Use Case #3 : Automatic Route Update

- **Summary:** 환승 지점에서 하차 안내 후 카운트다운을 거쳐(또는 사용자 확인 시) 다음 안내로 전환하는 기능
- **Primary Actor:** System
- **Preconditions:** 환승이 포함된 경로이며 환승 지점 하차 알림이 발생한 이후여야 한다.
- **Main Success Scenario:**
  1. 환승 지점 하차 알림과 함께 환승 안내 바텀 시트가 표시된다.
  2. 카운트다운(기본 8초)이 진행되며, 사용자는 '갈아탔어요'로 즉시 진행할 수도 있다.
  3. 카운트다운이 끝나거나 사용자가 확인하면 다음 구간 안내로 화면과 상태를 전환한다.
  4. 전환 후에는 새 구간의 정류장 좌표로 실시간 GPS 추적을 이어간다.

---

## 3. Domain Analysis

### 1) [그림 3-1] Domain class diagram

![domainclass](images/domain_class_diagram_v3.png)

### 2) [표 3-1] Description of Domain Classes

| Class Name | Description | Attributes (Public) |
| --- | --- | --- |
| **User** | 지역 설정을 관리하는 주체. 실시간 위치는 GPS_Sensor가 담당한다. 별도 계정·로그인 없이 기기 단위로 동작한다 | `selectedRegion` |
| **Route** | 전체 이동 경로 데이터 (자동 전환 로직 포함) | `totalDistance`, `currentSegmentIndex`, `transferCount` |
| **RouteSegment** | 한 번의 버스 탑승 구간. 환승마다 다음 구간으로 전환되는 단위 | `segmentIndex`, `transportType`, `lineName` |
| **Station** | 알림 및 자동 전환의 기준이 되는 지리적 지점 | `stationName`, `latitude`, `longitude`, `isTransfer` |
| **Alert** | 사용자에게 제공되는 시각/촉각 피드백 | `alertLevel`, `vibrationPattern`, `messageText` |

---

## 4. Functional Decisions

### 1) 역 검색 자동완성 여부: 예

역 검색 시 자동완성 기능을 제공하여 사용자가 입력하는 동안 실시간으로 관련 역 목록을 제안한다. 이는 노인 사용자의 입력 편의성을 높인다.

### 2) 주소-역 매핑 방식: ODsay 역/정류장 이름 검색

사용자가 입력한 역/정류장 이름을 ODsay 역 검색(searchStation)으로 질의하여 좌표가 포함된 후보 목록을 받는다. 별도의 주소 지오코딩·리버스 지오코딩은 사용하지 않으며, 0건일 때는 접미사(역·동·시장 등)를 떼고 1회 재검색한다.

### 3) 로컬 저장: 최근 검색 역 목록과 음성·진동 설정

최근 검색된 역 목록(지역별)과 피드백 설정(음성 안내 on/off, 선택 시 진동 on/off, 음성 크기 보통/크게, 음성 속도 보통/천천히)을 localStorage에 저장한다. 설정은 지역·입력 화면 헤더의 설정 버튼(바텀 시트)에서 변경할 수 있다. 단, 하차·환승·도착 등 안전 알림 진동은 설정과 무관하게 항상 동작한다. 경로 데이터 캐싱이나 오프라인 모드는 제공하지 않으며, 키가 없거나 호출이 실패하면 대구 지역 데모 데이터로 폴백한다.

### 4) 데이터 모델

- **역 정보:** 역 이름, 역 ID, 위도/경도, 지역(도/구) 정보
- **입력 필드:** 사용자가 입력하는 텍스트, 자동완성 제안 목록
- **API 응답 형식 예시:** 역 목록 배열 (예: `[{id: "123", name: "역삼역", lat: 37.5, lng: 127.0, cls: "subway", region: 1000}]` — `cls`는 버스/지하철 구분, `region`은 ODsay 도시코드 CID)

### 5) API 연계

- **외부 API 선택:** ODsay API의 역 검색(searchStation) 및 경로 탐색(searchPubTransPathT) 엔드포인트
- **요청 파라미터 형식과 응답 스키마:** 검색 쿼리 (역 이름 또는 주소), 응답으로 역 목록과 좌표 반환
- **수단 한정(지상 버스):** 경로 탐색은 `SearchPathType=2`(버스)로 호출하여 지상 대중교통만 안내한다. 지하철역 이름도 검색 결과에 노출되지만, 이는 **출발/도착 위치 지정용**(검색 행에 `이 위치에서 버스로 안내` 캡션 표시)일 뿐 실제 안내는 버스로 이루어진다.
- **근거리 보완:** 출발·도착 직선거리가 약 700m 미만이면 ODsay가 -98(너무 가까움)로 경로를 주지 않는다. 이 경우 두 정류장의 경유 노선(busStationInfo)을 교집합해 같은 방향(출발 정류장 인덱스 < 도착 정류장 인덱스)인 버스를 찾아 한 정거장 직행 경로를 직접 구성한다(구간 정류장은 노선 정차 순서 busLaneDetail로 확보).
- **에러 처리:** 미검색 시 "검색 결과가 없습니다. 다른 이름으로 검색해 보세요" 안내, 키가 없거나 호출 실패 시 대구 지역 데모 데이터로 폴백

### 6) UI/UX

- **입력창 디자인:** 큰 글자와 단순 프롬프트 ("출발역을 입력하세요"), 자동완성 드롭다운을 입력창 바로 아래에 위치
- **역 선택 시 처리:** 선택한 역을 출발/도착 입력 필드에 채우고 검색 패널을 닫으며, 출발지·도착지가 모두 정해지면 `경로 찾기` 버튼으로 다음 단계에 진행
- **노인 사용자 고려:** 입력 시 진동 피드백, 선택 시 명확한 음성/시각 신호 (예: "역삼역을 선택했습니다")

---

## 5. User Interface Prototype

### 1) UI Flow Diagram

지역 선택 → 출발지/도착지 입력 + 최근 검색 → 추천 경로 및 기타 경로 → 안내 페이지(실시간 위치/통합 바) → 동일 페이지에서 곧 내리세요 / 지금 내리세요

### 2) Screen Prototypes Description

- **Screen 1: Region Selection**
  ![Region Selection](images/regionSelect.png)
  - 지역 선택 버튼을 크게 배치하여 한눈에 서비스 가능 지역을 고를 수 있다.
  - 서울·인천·부산·대구·광주·대전·울산·세종 등 현재 지원하는 광역시가 명확히 표시된다.

- **Screen 2: Route Input**
  ![Route Input](images/routeInput.png)
  - 출발지와 도착지를 순서대로 입력하는 화면.
  - 입력창 바로 아래에 자동완성 제안이 표시되어 입력 부담을 줄인다.
  - 최근 검색 목록을 함께 보여줘서 자주 사용하는 역을 빠르게 선택할 수 있다.
  - 검색 결과가 없을 때는 `검색 결과가 없습니다. 다른 이름으로 검색해주세요.` 메시지를 보여준다.

- **Screen 3: Route Recommendation**
  ![Route Recommendation](images/routeRecommendation.png)
  - 추천 경로를 우선으로 보여주고, 대체 경로를 함께 제시한다.
  - 각 경로별로 환승 횟수, 총 소요 시간, 예상 출발·도착 시각, 노선 번호, 도보 소요 시간(분), 총 거리(약 ○○km)를 요약해서 표시한다.
  - 출발·도착 시각과 남은 시간은 ODsay의 소요 시간을 현재 시각 기준으로 환산한 **예상값**이다(실시간 버스 위치·교통 정보는 반영하지 않음). 총 거리는 ODsay 실경로 거리이며, 없을 경우 정류장 좌표로 환산한다.
  - 사용자가 선택한 경로 아래에 `이 경로로 안내` 버튼을 배치하여 다음 단계로 자연스럽게 이동한다.

- **Screen 4: Navigation Guide**
  ![Navigation Guide](images/navigatioGuide.png)
  - 선택한 경로의 실시간 안내를 제공하는 화면.
  - 상단에 통합 경로 진행 바를 배치하고, 현재 위치를 시각적으로 표시한다.
  - 다음 정류장, 남은 정류장 수, 예상 도착 시간 등의 정보를 함께 보여준다.
  - 동일 화면에서 `곧 내리세요`와 `지금 내리세요` 상태를 텍스트/색상/진동으로 단계적으로 전환한다.

- **Screen 4-1 : Arrival Alarm**
  ![Arrival Alarm](images/routeAlarm.png)
  - 하차 직전 및 도착 시에 표시되는 알림 화면.
  - 바텀 시트가 위로 올라오고, 큰 글씨와 색상 변화로 시각적 주의를 유도한다.
  - 진동과 음성 안내를 함께 제공하여 사용자가 즉시 하차 준비를 할 수 있도록 돕는다.
  
- **Screen 4-2 : Arrival Alert**
  ![Arrival Alarm](images/routeAlert.png)
  - 최종 도착시 표시되는 알림 화면.
  - 화면 전체가 적색으로 변환, 큰 글씨와 색상 변화로 시각적 주의를 유도한다.
  - 확인 버튼을 통해 최종 경로 종료를 선언 할 수 있다.

---
## 6. Glossary
| 용어 | 설명 |
| --- | --- |
| **Geofencing** | 특정 위도/경도 반경 내 진입을 감지하여 알림을 트리거하는 가상 경계 기술 |
| **Bottom Sheet** | 화면 하단에서 위로 슬라이드되어 중요 정보를 전달하는 UI 컴포넌트 |
| **Linear Progress Bar** | 출발부터 도착까지의 전체 경로를 하나의 선으로 표현한 통합 진행 바 |
| **Auto-Transition** | 사용자 개입 없이 GPS 좌표 변화만으로 시스템 상태를 다음 단계로 전환하는 로직 |
| **Screen Wake Lock** | 안내 중 기기 화면이 자동으로 꺼지지 않도록 유지하여 알림이 멈추지 않게 하는 웹 표준 API |
| **ARIA Live Region** | 동적으로 갱신되는 알림 문구를 스크린리더가 자동으로 읽도록 표시하는 접근성 영역(`aria-live`) |
| **지나침(Overshoot) 알림** | 도착 반경을 건너뛰고 하차역을 지나친 것을 감지해 "다음 정류장에서 내리세요"로 알리는 보강 알림 |

---

## 7. References

1. **ODsay API:** 지상 대중교통 경로 및 위치 데이터 활용 가이드
2. **W3C Geolocation API:** 실시간 좌표 수집 표준 명세
3. **Google Web Storage:** 최근 검색 기록 및 사용자 설정 저장 방식 참조
4. **MDN Web Docs:** Vibration API를 활용한 촉각 피드백 구현 지침

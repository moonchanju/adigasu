![logo](images/logo.png)

# [Design Report] 어르신 지상 대중교통 보조 시스템: 어디가수?

## Revision History

| Revision date | Version # | Description | Author |
| --- | --- | --- | --- |
| 2026-05-23 | 0.0.1 | 초기 설계 문서 작성 | 문찬주 |
| 2026-05-30 | 0.0.2 | 클래스 관계 설정 및 시퀀스 다이어그램 추가 | 문찬주 |
| 2026-06-04 | 0.0.3 | 상태 머신 다이어그램 및 구현 요구사항 정리 | 문찬주 |
| 2026-06-11 | 0.0.4 | 안내 중 화면 유지(Wake Lock)·하차역 지나침 알림·스크린리더(aria-live) 반영 | 문찬주 |
| 2026-06-12 | 0.0.5 | 경로 총 거리 표시 추가·도착 시각 예상값 명시(문서·앱 정합) | 문찬주 |

---

## Contents

1. Introduction
2. Class Diagram
3. Sequence Diagram
4. State Machine Diagram
5. Implementation Requirements
6. Glossary
7. References

---

## 1. Introduction

우리는 목적지에 가기 위해 매일 대중교통을 이용한다. 그러나 신체적 노화로 주의력이 감소한 노인 사용자는 안내 방송을 놓치거나 복잡한 노선도에 당황하여 하차 시점을 놓치는 어려움을 자주 겪는다. 기존의 길찾기 앱은 대부분 젊은 층에 최적화되어 있어 노인 사용자가 직관적으로 하차 시점을 인지하기 어렵고, 지하 공간에서의 GPS 오차는 부정확한 정보를 제공할 위험이 크다.

이러한 문제를 해결하기 위해 우리는 위치 추적 신뢰도가 높은 **지상 대중교통**으로 범위를 한정하고, 노인의 인지 특성을 반영한 직관적인 UI와 강력한 진동 알림을 제공하는 시스템 **"어디가수?"**를 개발하였다. 이 시스템이 이루고자 하는 첫 번째 목표는 **하차 놓침 방지**이다. 실시간 GPS 위치와 목적지 좌표 사이의 거리를 계산하여 목적지 한 정거장 전과 도착 시점에 단계적으로 알림과 진동을 제공한다. 두 번째 목표는 **이동 부담 완화**이다. 환승 지점 하차 안내 후 카운트다운(기본 8초)을 거쳐 별도 조작 없이 다음 구간 안내로 자동 전환하며, 사용자가 '갈아탔어요'를 누르면 즉시 이어받을 수도 있다.

주 타겟은 대중교통 이용 중 하차 정보를 놓치기 쉬운 노인 사용자이며, 현재는 서울·인천·부산·대구·광주·대전·울산·세종 등 전국 광역시의 지상 대중교통(버스)을 지원한다. 아래는 Analysis와 Conceptualization에 이은 이 System 개발의 세 번째 단계인 **Design**에 관한 내용으로서, 실제 System 구현에 직접적으로 관여하는 모든 요소들의 윤곽을 확정하고 구체적으로 디자인해 나가는 내용을 다룬다. 본 문서의 모든 세부 사항은 직접적인 구현 시 소스코드상에서의 용례와 일치함을 목적으로 한다.

---

## 2. Class Diagram

아래의 그림은 "어디가수?" 시스템의 Class Diagram을 표현한 그림이다.

![Class Diagram](images/class-diagram.png)

> **구현 메모.** 본 시스템은 빌드 단계가 없는 함수형 React(컴포넌트·훅·모듈 + `window` 전역)로 구현되었다. 따라서 아래 클래스들은 별도의 `class` 인스턴스가 아니라 **논리적 책임 단위**이며, 각 메소드는 그 책임을 수행하는 실제 함수에 대응한다. 클래스별 실제 구현 위치(파일·함수)는 표 아래 **§2-1 구현 매핑**에 정리한다.

아래의 표는 위의 Class Diagram에서 표현한 Class들에 대한 설명이다.

| Class Name | Explanation |
| --- | --- |
| **RegionSelector** | 앱 실행 시 서비스 가능 지역(서울·인천·부산·대구·광주·대전·울산·세종 등 광역시)을 큰 버튼으로 보여주고 선택을 받는 클래스이다. <br>- `showRegions()` → 구현: `RegionScreen` 컴포넌트가 `REGIONS`를 큰 버튼으로 렌더(전용 함수 없이 컴포넌트로 실현). <br>- `selectRegion(region)` → 구현: `App.selectRegion(r)` — 선택 지역 저장 후 입력 화면(`InputScreen`)으로 전환(이름 일치). |
| **RouteSearchUI** | 출발지와 도착지를 입력받는 GUI 클래스이다. 입력 중 자동완성을 보여주고 최근 검색 목록을 제공한다. <br>- `onInput(keyword)` → 구현: `SearchPanel`의 `q` 상태 + 280ms 디바운스 `useEffect`가 `searchStations` 호출(전용 메소드 없이 effect로 실현). <br>- `showAutoComplete(candidates)` → 구현: `results` 상태를 `Row`로 렌더. <br>- `requestRoute(origin, dest)` → 구현: `App.doSearch()` (이름 변경: requestRoute→doSearch). |
| **LocationService** | 사용자가 입력한 역/정류장 이름을 ODsay 역 검색으로 질의하여 좌표가 포함된 후보 목록으로 변환하는 클래스이다. 내부적으로 ApiClient를 사용한다. (별도 주소 지오코딩·리버스 지오코딩은 사용하지 않는다.) |
| **RouteCalculator** | 출발지·도착지 좌표를 기반으로 외부 API를 호출하여 지상 대중교통 중심의 경로를 탐색하는 클래스이다. <br>- `searchRoutes(origin, dest)` → 구현: `searchRoutes(origin, dest)` (api.jsx, 이름 일치). <br>- `sortByTransfer(routes)` → 구현: `mapOdsayPaths()` 내부 인라인 `paths.sort()`(환승 적은 순→시간순)로 실현(전용 함수 아님). |
| **Route** | 전체 이동 경로 데이터를 담는 클래스이다. 여러 개의 RouteSegment(환승 구간)로 구성되며 현재 진행 중인 구간 인덱스를 가진다. <br>- `moveToNextSegment()` → 구현: `doTransfer()` (navigation.jsx, `setLegIdx(l=>l+1)`; 이름 변경). <br>- `isLastSegment()` → 구현: `isLastLeg` 불리언 변수(`legIdx === route.legs.length-1`)로 실현(메소드 아님). |
| **RouteSegment** | 하나의 노선 탑승 구간을 나타내는 클래스이다. 탑승역, 하차역, 정류장 수, 노선명을 가진다. |
| **Station** | 알림과 자동 전환의 기준이 되는 지리적 지점(정류장/역)을 나타내는 클래스이다. 역 ID, 이름, 위도/경도, 지역 정보를 가진다. |
| **NavigationManager** | 실시간 안내의 핵심 클래스이다. LocationTracker로부터 위치를 받아 GeofenceChecker로 거리를 판단하고, 조건 충족 시 AlertManager로 알림을 발생시키며 진행 바를 갱신한다. 안내가 진행되는 동안에는 화면 잠금(Wake Lock)을 유지하여 화면이 꺼져 알림이 멈추는 것을 막는다. <br>- `startNavigation()` → 구현: `NavScreen` 마운트 시 `useGeolocation(mode==='gps')` 구독으로 시작하고 `useWakeLock(true)`로 화면 유지(전용 함수 없이 컴포넌트로 실현). <br>- `onLocationUpdate(loc)` → 구현: `geo.coords` 변경 `useEffect`가 `geofenceLeg`로 거리 판단 후 `arriveAt(target)` 호출(진행·알림·자동 전환); 핵심 함수명은 `arriveAt`. 하차역 최근접 거리(`minAlight`)를 추적해, 도착 반경을 건너뛰고 정류장을 지나치면 지나침 알림을 발생시킨다. |
| **LocationTracker** | Geolocation API를 사용하여 실시간 GPS 위치를 추적하는 클래스이다. `startTracking()` → 구현: `useGeolocation(enabled)` 훅(`navigator.geolocation.watchPosition` 구독/해제; 이름 변경)으로 위치 변경을 구독하고, 갱신마다 `NavScreen`에 좌표를 전달한다. |
| **Location** | 한 시점의 위도/경도와 타임스탬프를 담는 값 객체이다. |
| **GeofenceChecker** | 현재 위치와 목표 정류장 사이의 거리를 계산하고, 예고 알림(한 정거장 전)·도착 알림·지나침(하차역을 건너뛰고 통과) 조건을 판단하는 클래스이다. |
| **AlertManager** | 사용자에게 시각·촉각 피드백을 제공하는 클래스이다. 예고 알림(바텀 시트 슬라이드 업 + 예고 진동), 최종 알림(화면 색상 반전 + 연속 진동), 지나침 알림(하차역을 지났을 때 "다음 정류장에서 내리세요")을 담당한다. 모든 알림 문구는 음성(Web Speech)과 함께 화면에 숨겨진 `aria-live` 영역으로도 출력되어 저시력 사용자의 스크린리더(보이스오버/톡백)가 즉시 읽어 준다. |
| **Alert** | 사용자에게 표시되는 알림의 메시지·진동 패턴·종류를 담는 데이터 클래스이다. |
| **StorageManager** | Web Storage(localStorage)에 최근 검색 역 목록과 피드백 설정(음성 안내·진동·음성 크기/속도)을 저장하고 불러오는 클래스이다. |
| **ApiClient** | 외부 API(ODsay) 호출을 담당하는 공통 클래스이다. 호출은 서버 프록시(`/api/*`)를 경유하며, 키가 없거나 호출이 실패하면 에러 코드를 반환해 호출부가 데모 데이터로 폴백한다. |

### 2-1. 구현 매핑 (Class → 실제 코드)

아래 표는 위 논리 클래스들이 실제 소스코드의 어느 파일·함수/컴포넌트로 실현되는지를 정리한 것이다.

| 논리 클래스 | 실제 구현 (파일 · 함수/컴포넌트) |
| --- | --- |
| **RegionSelector** | `screens.jsx` `RegionScreen` (`REGIONS` 렌더) · `main.jsx` `App.selectRegion(r)` (지역 저장·화면 전환, 지역 변경 시 입력 초기화) |
| **RouteSearchUI** | `screens.jsx` `InputScreen` + `SearchPanel` (입력 디바운스 280ms → `searchStations`, 결과 `Row` 렌더, 역 선택 시 `vibrateFeedback()`·`speak()` 피드백) · 경로 요청 `main.jsx` `App.doSearch()` |
| **LocationService** | `api.jsx` `searchStations(q, region)` (ODsay `searchStation`, 0건 시 접미사 제거 재검색) · 폴백 `screens.jsx` `staticStations(t)` |
| **RouteCalculator** | `api.jsx` `searchRoutes(origin, dest)` · `mapOdsayPaths()` (버스 구간만 추출; 환승 적은 순→시간순 `sort` = `sortByTransfer`) · 근거리 -98 시 `shortBusRoute()`로 한 정거장 직행 버스 직접 구성 |
| **Route** | route 객체(`legs[]`, `transfers`, `durationMin`, `distanceM`=총거리) · 총 거리 표시 `screens.jsx` `routeKm(rt)`(실거리 없으면 좌표로 환산) · 다음 구간 전환 `navigation.jsx` `doTransfer()`/`setLegIdx` · 마지막 구간 판정 `isLastLeg` |
| **RouteSegment** | leg 객체 `{ mode, line, color, stops[] }` (`data.jsx` `ROUTES` / `api.jsx` 변환 결과) |
| **Station** | 역 객체 `{ name, lat, lng, id, cls }` · 좌표 사전 `COORDS` (`data.jsx`) |
| **NavigationManager** | `navigation.jsx` `NavScreen` — 마운트 시 추적 시작 + `useWakeLock(true)`(화면 유지), `geo.coords` 변화 useEffect → `geofenceLeg` → `arriveAt(target)`로 진행·알림·자동 전환, 하차역 최근접(`minAlight`) 추적으로 지나침 감지 |
| **LocationTracker** | `geo.jsx` `useGeolocation(enabled)` 훅 (`navigator.geolocation.watchPosition` 구독/해제) · 화면 유지 `useWakeLock(active)` 훅(Screen Wake Lock; 미지원 기기는 무시) |
| **Location** | `geo.coords {lat,lng}` + `accuracy` (Geolocation 이벤트 좌표를 직접 사용; 별도 timestamp 값 객체는 두지 않음) |
| **GeofenceChecker** | `geo.jsx` `geofenceLeg(stops, coords)` · `distM(a,b)`(Haversine) · 임계값 `GEO { ARRIVE_R:70, PRE_R:450, PASS_R:130, PASS_HYST:50 }` (PASS_R/PASS_HYST = 지나침 판정) |
| **AlertManager** | `navigation.jsx` `firePre()`/`fireTransfer()`/`fireFinal()`/`firePassed()`(지나침) + `announce()`(음성 `speak()` + `aria-live` 라이브 영역 동시 출력) + `vibrate()`(안전 알림 — 설정 무관 항상) + 시트 컴포넌트 `PreAlert`/`TransferSheet`/`FinalAlert`(후자 둘은 `passed`일 때 "지나쳤어요" 변형). (역 선택 등 보조 진동은 `vibrateFeedback()`로 분리 — 설정 시에만) |
| **Alert** | 별도 클래스 없음 — 진동 패턴·문구를 함수 인자로 인라인 전달(예: `vibrate([200,100,200])`, `speak('곧 내리세요…')`) |
| **StorageManager** | `main.jsx` `App` localStorage 로직 — 초기 `saved` 로드, `useEffect`로 `{ recents, soundOn, vibrateOn, voiceVol, voiceRate }` 저장, `addRecent()`/`removeRecent()`·설정 변경 `changeSetting()`(→ `geo.jsx` `setFeedbackSettings`로 피드백 엔진 동기화) |
| **ApiClient** | `server.js` 프록시(`odsay()`, `/api/health`·`/api/stations`·`/api/routes`·`/api/station-buses`·`/api/bus-lane`) + `api.jsx` `apiAvailable()`·`fetch`·근거리 직행버스 탐색 `shortBusRoute()` (ODsay 키는 서버에만 보관) |

---

## 3. Sequence Diagram

아래에 나오는 그림들은 Analysis에서 표현한 핵심 Use Case들을 Sequence Diagram으로 표현한 그림들이다.

### 1) Search & Select Route (경로 검색 및 선택)

사용자가 출발지/도착지를 입력하면 LocationService가 좌표로 변환하고, RouteCalculator가 외부 API를 호출하여 경로를 탐색한 뒤 환승이 적은 순으로 정렬하여 보여준다.

![Sequence Diagram - Search & Select Route](images/sequence-search-route.png)

### 2) Start Navigation & Track Location (안내 시작 및 위치 추적)

사용자가 경로를 선택하고 안내를 시작하면 NavigationManager가 LocationTracker를 가동하여 실시간 위치를 추적하고 진행 바를 갱신한다.

![Sequence Diagram - Start Navigation & Track Location](images/sequence-start-navigation.png)

### 3) Provide Arrival Alert (도착 알림 - 지오펜싱)

실시간으로 계산된 거리가 임계값에 도달하면 단계적으로 예고 알림과 최종 알림을 발생시킨다. 만약 버스가 도착 반경(70m)을 건너뛰어 하차역을 지나치면(하차역 최근접 거리가 한 번 가까워졌다가 다시 멀어지면), 지나침 알림으로 "다음 정류장에서 내리세요"를 안내한다. 모든 알림은 음성과 함께 `aria-live` 영역으로도 출력되어 스크린리더 사용자에게 전달된다.

![Sequence Diagram - Provide Arrival Alert](images/sequence-arrival-alert.png)

### 4) Automatic Route Update (환승 자동 전환)

환승 지점 하차 알림 이후, 카운트다운(기본 8초)이 끝나거나 사용자가 '갈아탔어요'를 누르면 다음 안내 구간으로 전환하고, 전환 후에는 GPS 위치로 새 구간을 추적한다.

![Sequence Diagram - Automatic Route Update](images/sequence-route-update.png)

---

## 4. State Machine Diagram

아래의 그림은 "어디가수?" 시스템의 State Machine Diagram을 표현한 그림이다.

![State Machine Diagram](images/state-machine.png)

아래는 위의 State Machine Diagram에 나온 각 State들에 대해 간단하게 설명한 표이다.

| Status | Explanation |
| --- | --- |
| **RegionSelection** | 앱이 실행된 직후 서비스 가능 지역을 선택하는 상태이다. 지역을 선택하면 RouteInput으로 전환된다. |
| **RouteInput** | 출발지와 도착지를 입력하는 상태이다. 입력 중 자동완성과 최근 검색을 제공한다. |
| **Searching** | 외부 API를 호출하여 경로를 탐색하는 상태이다. 결과가 없으면 경로 화면에서 빈 상태 안내(다시 입력하기)를 표시하고, 성공하면 RouteRecommendation으로 간다. |
| **RouteRecommendation** | 탐색된 경로를 환승이 적은 순으로 정렬하여 보여주는 상태이다. 사용자가 경로를 선택하면 안내가 시작되며, 결과가 없으면 빈 상태 안내를 표시하고 '다시 입력하기'로 RouteInput으로 돌아간다. |
| **Navigating** | 실시간 위치를 추적하며 진행 바와 남은 정류장 수를 갱신하는 핵심 상태이다. 위치 갱신 이벤트마다 거리를 판단한다. |
| **PreAlert** | 하차역 약 450m 전(데모 모드에서는 한 정거장 전), 바텀 시트가 슬라이드 업되며 예고 진동을 발생시키는 상태이다. |
| **AutoTransition** | 환승이 포함된 경로에서 하차 안내 후 카운트다운(기본 8초)이 끝나면 사용자 조작 없이 다음 구간으로 전환하는 상태이다(사용자가 직접 확인할 수도 있다). 환승역을 지나친 경우에도 이 상태로 진입하며 "갈아탈 곳을 지나쳤어요"로 문구가 바뀐다. |
| **FinalAlert** | 목적지 도착 시 화면 색상을 반전하고 강력한 연속 진동을 발생시키는 상태이다. 사용자가 확인하면 안내가 종료된다. 도착 반경(70m)을 건너뛰고 하차역을 지나친 경우에도 이 상태로 진입하며, "지나쳤어요 · 다음 정류장에서 내리세요"로 안내한다(지나침 알림). |

---

## 5. Implementation Requirements

"어디가수?" 시스템을 구동하기 위해 필요한 요구사항은 아래와 같다.

### 1) Hardware Requirements

| 항목 | 요구사항 |
| --- | --- |
| Device | GPS를 지원하는 스마트폰 (Android / iOS) 또는 위치 권한이 가능한 기기 |
| Sensor | GPS, 진동(Vibration) 모터 지원 |
| Network | 3G/LTE/Wi-Fi 등 데이터 통신 연결 필요 (역 검색·경로 탐색에 상시 통신 필요) |
| RAM | 2GByte 이상 권장 |

### 2) Software Requirements

| 항목 | 요구사항 |
| --- | --- |
| Operating System | Android 8.0 이상 / iOS 13 이상 |
| Runtime | 모던 웹 브라우저(Chrome, Safari) 또는 WebView 환경 |
| Implementation Language | JavaScript (ES6 이상) / HTML5 / CSS3 |
| Required API | W3C Geolocation API, Vibration API, Web Speech API(음성 안내), Web Storage API, Screen Wake Lock API(안내 중 화면 유지), WAI-ARIA Live Region(스크린리더 알림) |

### 3) Nonfunctional Requirements

- 본 시스템은 경량 Node 프록시 서버를 통해 외부 Open API(ODsay)를 호출하여 경로·위치 데이터를 수집한다(키 보호·CORS 우회). 최근 검색 역 목록과 피드백 설정(음성 안내·진동·음성 크기/속도)은 Web Storage(localStorage)에 저장된다.
- 지하 구간 등 GPS 신뢰도가 낮은 환경을 고려하여 **지상 대중교통**으로 범위를 한정한다.
- 노인 사용자를 고려하여 큰 글씨, 단순한 화면, 강한 진동·색상 대비를 기본으로 한다. 저시력 사용자를 위해 모든 알림 문구를 `aria-live` 영역으로 출력하여 OS 스크린리더가 읽도록 한다.
- 외부 API 키가 없거나 호출이 실패하면 대구 지역 데모 데이터로 폴백한다.
- 백그라운드 위치 추적은 OS 정책에 따라 제한될 수 있으므로, 안내 중에는 Screen Wake Lock으로 화면을 켜진 상태로 유지하고 화면 점유형 알림으로 보완한다(Wake Lock 미지원 기기는 화면 점유형 알림에만 의존).

---

## 6. Glossary

| Terms | Description |
| --- | --- |
| **Class Diagram** | 객체지향 시스템 설계에서 시스템의 논리 설계를 위한 클래스들의 존재와 그들의 관계를 도식으로 정의한 것. |
| **Sequence Diagram** | 객체들 사이에 주고받는 메시지의 시간 순서를 표현하여 기능의 동작 흐름을 보여주는 다이어그램. |
| **State Machine Diagram** | 시스템이 한정된 수의 상태로 구성됨을 전제로, 이벤트에 따른 상태 전이를 표현하여 동작을 설명하는 다이어그램. |
| **Geofencing** | 특정 위도/경도 반경 내 진입을 감지하여 알림을 트리거하는 가상 경계 기술. |
| **Geocoding** | 주소나 장소 이름을 위도/경도 좌표로 변환하는 것. 반대 변환은 Reverse Geocoding이라 한다. |
| **Bottom Sheet** | 화면 하단에서 위로 슬라이드되어 중요 정보를 전달하는 UI 컴포넌트. |
| **Auto-Transition** | 사용자 개입 없이 GPS 좌표 변화만으로 시스템 상태를 다음 단계로 전환하는 로직. |
| **Screen Wake Lock** | 앱이 활성인 동안 기기 화면이 자동으로 꺼지지 않도록 유지하는 웹 표준 API. 안내 중 화면 꺼짐으로 알림이 멈추는 것을 막는다. |
| **ARIA Live Region** | 화면에 동적으로 갱신되는 내용을 스크린리더가 자동으로 읽도록 표시하는 접근성(WAI-ARIA) 영역(`aria-live`). 본 시스템은 알림 문구를 시각적으로 숨긴 라이브 영역에 출력한다. |
| **지나침(Overshoot) 알림** | 도착 반경(70m)을 건너뛰고 하차역을 지나친 경우, 최근접 거리가 다시 멀어지는 것을 감지해 "다음 정류장에서 내리세요"로 알리는 보강 알림. |
| **메소드** | 멤버 함수라고도 하며, 객체지향 프로그래밍 언어에서 클래스 혹은 객체에 소속된 서브루틴을 가리킨다. |

---

## 7. References

1. **ODsay API** — 지상 대중교통 경로 및 위치 데이터 활용 가이드. https://www.odsay.com
2. **W3C – Geolocation API** — 실시간 좌표 수집 표준 명세. https://www.w3.org/TR/geolocation-API/
3. **MDN Web Docs – Vibration API** — 촉각 피드백 구현 지침. https://developer.mozilla.org/docs/Web/API/Vibration_API
4. **Google Web Storage** — 최근 검색 기록 및 사용자 설정 저장 방식. https://developers.google.com/web/fundamentals/instant-and-offline/web-storage
5. **SPARX SYSTEMS – State Machine Diagram Practice.** http://www.sparxsystems.com.au/resources/uml2_tutorial/uml2_statediagram.html
